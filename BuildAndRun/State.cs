namespace BuildAndRun {
    /// <summary>
    /// L'état de l'automate.
    /// Les nom sont parlant, donc je développe pas!
    /// </summary>
    public enum State {
        Unprocessed,
        Failed,
        Error,
        Success
    }
}
